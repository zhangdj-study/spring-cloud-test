server:
  port: 8002
spring:
  application:
    name: provider-ticket
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true #是否使用ip地址注册