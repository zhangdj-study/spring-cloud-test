server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server #实例名
  client:
    register-with-eureka: false #不注册自己
    fetch-registry: false #不从服务上获取注册信息
    service-url:
      defaultZone: http://localhost:8761/eureka